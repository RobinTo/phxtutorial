<table class="table">
  <%= for user <- @users do %>
    <tr>
      <td><%= first_name(user) %></td>
      <td><%= user.id %></td>
      <td><%= link "View", to: user_path(@conn, :show, user.id) %></td>
    </tr>
  <% end %>
</table>